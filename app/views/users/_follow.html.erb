<!--フォローボタン-->
<%= form_with url: relationships_path, remote: true do |f| %>
  <%= f.hidden_field :following_id, :value => @user.id %>
  <div class="profile-detail"><%= f.submit "フォローする", class: "btn btn-outline-secondary" %></div>
<% end %>
