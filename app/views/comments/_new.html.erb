  <%= form_with(model:[article, comment], url: article_comments_path(@article) ) do |f| %>
    <%= f.text_area :body, rows:'5',placeholder: "コメントをする（500文字以内）",maxlength:"500", class:"form-control" %>
    <%= f.submit "送信する", class:"btn btn-success" %>
  <% end %>
